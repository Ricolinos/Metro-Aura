# COMPAT_STUDIO.md — Checklist vivo de compatibilidad con Aura Studio

Estado real de cada ítem del checklist C1–C21 de
`docs/plans/PLAN_MAESTRO.md` §4. Se actualiza al cierre de cada fase
que lo toque. Fuente del contrato: `Aura-Firmware/CONTRATO-firmware-studio.md`
y `Aura-Firmware/docs/contracts/library-layout-v1.md` (no copiados a
este repo — se leen desde el repo hermano; el contrato es inmutable
desde este lado).

Leyenda: ⬜ pendiente · 🟡 parcial/no verificado · ✅ verificado en simulador · 🔒 verificado en hardware

**Nota sobre C32/C33 (contrato v18, ronda "homologación", 2026-09-04):**
`Aura-Firmware/CONTRATO-firmware-studio.md` todavía dice "Versión 17" al
cierre de la Fase 1 de esta ronda — la sesión canónica (Aura-Firmware) aún
no ha publicado v18. Las dos filas se implementaron contra el texto literal
de `docs/plans/PLAN-ronda-3-firmwares-maestro.md` §A.2, que es la
especificación acordada para v18 y de donde saldrá el texto del contrato.
Cuando el repo hermano publique v18, esta nota se borra y las filas citan el
contrato como el resto.

| # | Requisito | Estado | Fase | Nota |
|---|---|---|---|---|
| C1 | `/.rockbox/aura/` existe tras el primer arranque | ✅ | F6 | `metro_settings_load()` llama `metro_settings_save()` la primera vez que `aura.cfg` no existe, creando `/.rockbox/aura/` con `mkdir()` si hace falta. Verificado: `docs/screenshots/F6-aura-cfg.txt` es el archivo real generado en un simdisk limpio |
| C2 | `aura.cfg` existe y se regenera entero en cada guardado | ✅ | F6 | `metro_settings_save()` (`creat()`, nunca edita en sitio) — mismo patrón que `aura_settings_save()` |
| C3 | `aura.cfg` con `sync_marker_supported: 1` y `firmware_family: metro`, sin `theme_format_supported` | ✅ | F6 | `docs/screenshots/F6-aura-cfg.txt`: ambas claves presentes, `theme_format_supported` ausente (Metro no tiene sistema de temas instalables, F2/M-012 ya lo dejó fuera de alcance) |
| C4 | Marcador `/.aura/sync-pending.json` procesado y borrado al terminar bien | ✅ | F6 | `docs/screenshots/F6-updating-library.png` (pantalla mientras el marcador de prueba tenía `music: true`) — verificado que el archivo desaparece de `simdisk/.aura/` tras un `tagcache_update()` exitoso |
| C5 | Marcador con `version` no soportada se deja intacto | 🟡 | F6 | Implementado (`metro_sync_check_pending()`, caso `METRO_SYNC_MARKER_UNSUPPORTED` → `METRO_SYNC_ERROR_VERSION`, el marcador nunca se borra en ese camino) y cubierto por los tests host-side de `metro_sync_marker.c` (`test_newer_version_is_unsupported_but_readable`) — no se capturó una pantalla del estado de error específicamente, solo se verificó la ruta feliz en sim |
| C6 | `metro_disk_handoff()` corre al volver de USB | 🟡 | F9 | El código ya existe desde F6 (`metro_main.c`: `default_event_handler(action) == SYS_USB_CONNECTED` → `metro_disk_handoff()`) pero no se pudo ejercitar en sim — hace falta el modo de prueba `METRO_SIM_FORCE_USB=1` que F9 agrega a `sim_tasks.c`. Queda con el estado de F9 hasta que esa herramienta exista |
| C7 | Claves `rtc_sync_*` se aplican y se descartan | ✅ | F6 | `metro_settings_apply_pending_clock()`: si las 6 claves transitorias están, escribe el RTC (`rtc_write_datetime()`, bajo `#if CONFIG_RTC`) y llama `metro_settings_save()`, que las descarta solas al regenerar `aura.cfg` desde `metro_settings_t` (esas claves nunca son parte del struct). No verificado en sim (el simulador no tiene RTC real que inspeccionar), pero la lógica de lectura/aplicación/descarte es la misma que `aura_settings_apply_pending_clock()`, ya probada en producción |
| C8 | Tagcache indexa los 3 layouts de `/Music/` | ✅ | F4 | `firmware/tools/gen_test_media.sh` genera pistas en los 3 layouts (`Artista/Álbum/archivo`, `Álbum/archivo`, `Artista/archivo`); los 3 aparecen correctamente agrupados en `docs/screenshots/F4-artists.png`/`F4-artist-albums.png` ("Flat Album Test"/"Flat Artist Test" son los layouts sin carpeta de artista/álbum). Por diseño el firmware no distingue layouts (`tagcache_scan_paths = "/"`, ver `docs/contracts/library-layout-v1.md` de Aura-Firmware) — Metro no necesitó código propio para esto |
| C9 | `cover.jpg` de carpeta y carátula embebida (APIC) se muestran | 🟡 | F5 | `docs/screenshots/F5-nowplaying-art.png` verifica carátula de carpeta (`find_albumart()`); `F5-nowplaying-noart.png` verifica el tile de respaldo cuando no hay arte. Carátula embebida (APIC) NO se ejercitó con ningún fixture — mismo camino de código (`metro_albumart.c: decode_embedded()`), solo falta un `.mp3` de prueba con arte embebido en `gen_test_media.sh` |
| C10 | `.lrc` no rompe nada (sin mostrarse en v1) | ✅ | F4 | `metro-test.lrc` convive en `Music/` con las pistas de prueba en todas las corridas de F4; tagcache lo ignora (extensión no reconocida como audio), Metro no lo lee en ningún lado. Backlog: mostrarlo |
| C11 | `Playlists/*.m3u8` se listan y reproducen | ✅ | F4 | `docs/screenshots/F4-playlists.png` (listado) + verificado que seleccionar "QA Favorites" arranca reproducción real (pantalla "reproduciendo" con la primera pista de la lista) |
| C12 | `/Videos/*.mpg` con nombre límite (95 bytes) se lista y abre | ✅ | F7 | `docs/screenshots/F7-videos-all.png` (listado, orden natural vía `strnatcasecmp`) + `F7-mpegplayer.png` (SELECT abre `mpegplayer.rock` de verdad, no un doble) — verificado que MENU dentro del plugin vuelve a la lista en la misma fila seleccionada |
| C13 | `/Photos/*.jpg` ≤640px se listan (hasta 500) y abren | 🟡 | F7 | `docs/screenshots/F7-photos.png`: filtro de extensión verificado (`photo3.bmp`/`photo4_unsupported.png` correctamente excluidos, solo `.jpg` listado). Apertura con `imageviewer.rock` usa el mismo mecanismo `plugin_load()` que `metro_video_play()`, ya probado ahí, pero no se capturó una pantalla abriendo una foto específicamente |
| C14 | Categorías de video/foto: presentes → pivots; ausentes → solo "all" | ✅ | F7 | `docs/screenshots/F7-videos-movies.png` (con `video_categories.cfg`, pivots "peliculas/series/clips" aparecen) vs. `F7-videos-nocfg.png` (mismo simdisk, `video_categories.cfg` removido a propósito para la captura: solo "todos") — mismo mecanismo de índice para fotos (`photo_categories.cfg`), no un camino de código aparte |
| C15 | `sync_summary.cfg`: presente → conteos; ausente → guiones | ✅ | F8, completado R2-F1 | `docs/screenshots/F8-about.png` (ausente, un solo renglón "not synced yet" — desviación deliberada de legibilidad frente a "3 filas con guiones", ver `docs/DESVIACIONES.md` F8-1, sigue vigente). R2-F1/DD-5 (M-055) extendió el parseo de 3 a las 13 claves reales que Aura Studio escribe (`music_bytes`/`video_bytes`/`photo_bytes`/`playlist_count`/desglose de categoría) — `docs/screenshots/R2-F1-about-full.png`, `R2-F1-about-full-scrolled.png`, `R2-F1-about-full-end.png` verifican las 13 filas con un `sync_summary.cfg` completo colocado a mano |
| C16 | `device.cfg`: presente → nombre; ausente → genérico | ✅ | F8 | `docs/screenshots/F8-about.png` (ausente → "my ipod") vs. variante con `device.cfg` verificada a mano (presente → "iPod de Prueba", nombre real) |
| C17 | Archivos no usados en v1 (`themes/`, `sync_manifest.json`) no rompen nada | ✅ | F7-F8 | Ninguno de estos dos se lee en ningún punto del árbol de `apps/metro/` (verificado por inspección: ningún módulo abre esas rutas) — su sola presencia en disco no puede afectar a Metro, ausencia total de código que los toque. `artists/` (R3-F3/C25) y `ratings.cfg` (R3-F5/C26) **dejaron** esta fila al empezar a usarse -- ver esas filas |
| C18 | Ninguna ruta/clave del contrato se escribe con otro nombre; nada se borra salvo el marcador al terminar | ✅ | continuo | Revisado en cada fase que tocó `metro_settings`/`metro_sync`/`metro_device`: las únicas escrituras de Metro son `aura.cfg` (F6, claves de la tabla D), el marcador (F6/F8, solo su propio `attempts`/borrado al terminar bien) — ninguna clave/ruta ajena se toca |
| C19 | Descriptor USB sin cambios (stack Rockbox stock) | ✅ | F0 | F0 no toca `usb-s5l8702.c` ni strings de identidad USB — confirmado por el diff de F0 (ningún archivo USB en la lista de 10 portados) |
| C20 | `AuraDeviceProbe` clasifica `.aura(hasBooted:true)` con Studio real | ⬜ | F13 | Requiere hardware |
| C21 | Advertencia documentada sobre `AuraUpdateChecker` | ✅ | F13 | `docs/ESTADO_FINAL.md`, sección "Advertencia: riesgo con Aura Studio (M-004)" |
| C22 | `version.txt` en `rockbox.zip`, solo en releases reales | ✅ | R2-F1 | Ítem nuevo de ronda 2 (no forma parte del checklist original C1-C21 de `PLAN_MAESTRO.md` §4). `package_dist.sh --release-tag vX.Y.Z` (DD-6, M-056) escribe `.rockbox/aura/version.txt` con el tag exacto dentro de `rockbox.zip`, mismo contrato que Aura-Firmware — sin la bandera (build de desarrollo) no se escribe. Verificado en vivo: guardia de árbol sucio (`--release-tag` sobre árbol con cambios sin commitear → `ERROR`, sin compilar) y build de desarrollo completo (`version.txt` correctamente ausente del zip). La escritura del archivo en sí se verificó en aislamiento (ver `DECISIONS.md` M-056) — el camino feliz completo (`--release-tag` sobre árbol limpio) queda pendiente para el primer release real de R2-F5, ya que probarlo requiere que `package_dist.sh` mismo esté comiteado |
| C23 | `.../aura/metrocache/<fuente>/` (caché de miniaturas de cualquier cuadrícula de tiles) es un árbol propio de Metro, ajeno al contrato — Studio nunca debe leerlo, escribirlo ni tratarlo como una violación de esquema | ✅ (superado por C30) | R2-F2/R3-F1 | **v15 (M-096): la caché se mudó a `/.aura/thumbs/<fuente>/` (compartida entre familias, fila C30); `metrocache/` ya no existe — se renombra al arrancar o se borra si la compartida ya existía.** Histórico: ítem nuevo de ronda 2 (DD-9, M-057), generalizado en R3-F1/DD-1 (M-062): `metro_photo_thumbs.c` (un directorio fijo, `metrocache/photos/`) pasó a `metro_thumbs.c` (motor genérico, un subdirectorio de `metrocache/` por fuente — `photos/` hoy, `artists/`/`albums/` se suman en R3-F3/R3-F4) para no triplicar la misma caché. Mismo principio que `photocache/` de Aura-Firmware en cada subdirectorio: interno al firmware, Studio "ignora directorios que no conoce". Formato/tamaño propios de Metro en todos (raw `fb_data` 80×80, nombre `<clave-estable>.<mtime>.mth`) — deliberadamente **no** el mismo formato que `photocache/`/`cfcache/` de Aura (ver `docs/DESVIACIONES.md` R2-2), y la limpieza de convivencia entre familias (R2-F6+, Aura-Studio) borra `metrocache/` completo igual que borraría `photocache/`/`cfcache/` al cambiar de familia en el mismo dispositivo. Verificado en R3-F1: `ls .rockbox/aura/metrocache/photos/` en el simulador tras generar la cuadrícula (con la caché previa borrada a mano para forzar regeneración) muestra 8 `.mth` con mtime en el nombre, idéntico esquema que R2-F2 |
| C24 | `.lrc` junto al audio (ruta hermana, mismo directorio, mismo nombre base) se consume para letras sincronizadas en Now Playing | ✅ | R3-F2 | Ítem nuevo de ronda 3 (DD-2/DD-3, M-063). Contrato `library-layout-v1.md` §3 (v1.3): UTF-8 sin BOM, ≤8 KB, ≤600 líneas con timestamp — Studio ya escribe estos archivos hoy (`LibrarySync.swift`), Metro los consume tal cual sin negociar formato. `metro_lrc.c` (módulo propio, no port de `aura_lrc.c` — ver `docs/DESVIACIONES.md` R3-1 para el modelo de memoria) deriva la ruta hermana y parsea; `metro_screen_nowplaying.c` los carga con el mismo patrón "caché de 1 por `id3->path`" que ya usa `metro_albumart.c`. Ausencia del archivo, archivo vacío o sin líneas válidas: la fila "letra" de Options queda en "no disponible" y el modo nunca se muestra (nunca una pantalla de letras en blanco) |
| C25 | `.../aura/artist_images.cfg` + `.../aura/artists/<archivo>.jpg` (índice + fotos de artista de Studio) se consumen para la cuadrícula de Artistas | ✅ | R3-F3 | Ítem nuevo de ronda 3 (DD-6, M-064). Contrato `CONTRATO-firmware-studio.md` §D.3 (v6/v7): formato **invertido** `<archivo>.jpg: <tag de artista>` (el archivo es la clave porque es FAT-segura; el artista, que puede traer `:`, es el valor — comparación exacta contra `tag_artist`, nunca `tag_albumartist`, sin normalizar acentos). Parser propio host-testeable (`metro_artist_images_parse.c`, mismo patrón que Aura-Firmware usa para el suyo, consultado read-only) + índice con las dos reglas del contrato (valor duplicado → gana la primera línea; tope 300 entradas) en `metro_artist_images.c`; la lectura real del archivo y el escaneo de `artists/` (para el mtime de cada foto, clave de caché de `metro_thumbs.c`) viven en `metro_music.c`. Artista sin foto mapeada → tile de acento con inicial (mismo fallback que la cuadrícula ya trae, sin código nuevo para ese caso). Verificado en vivo con 3 fotos reales (incluida una cuyo tag de artista trae `:`) y 4 artistas sin foto mostrando el fallback correctamente — ver `docs/DESVIACIONES.md` R3-3 para un bug real de presupuesto de memoria encontrado y corregido en el camino |
| C26 | `.../aura/ratings.cfg` (`<ruta absoluta>: <rating 0-10>` por línea) se reimporta a `tag_rating` cada vez que el import de música termina bien | ✅ | R3-F5 | Ítem nuevo de ronda 3 (DD-7, M-066). **Asimetría real, de una sola vía**: Studio escribe `ratings.cfg`, Metro lo lee y aplica a `tag_rating` (motor nativo de Rockbox, no una tabla propia) — pero una calificación puesta **en el dispositivo** (fila "calificación" de Options en Now Playing, 5 estrellas, mapeadas ×2 al 0-10 nativo) **no tiene camino de vuelta hacia Studio**: se pierde silenciosamente en el siguiente sync si `ratings.cfg` no la conoce todavía (Studio la sobrescribe con lo que él cree que es la calificación, que puede ser nada). El contrato actual no resuelve esto del lado de Studio — documentado aquí explícitamente, no escondido; resolverlo de verdad (ida y vuelta real) queda fuera de esta ronda. `metro_sync.c` reimporta en `job_ended()`'s camino de éxito real (no en el atajo "sin música que sincronizar" de `start_job()`, que no tocó tagcache esta vez) — mismo punto temporal "al terminar bien" que usa Aura-Firmware (`library-layout-v1.md`, consultado read-only). Verificado en vivo: con `ratings.cfg` de muestra en el simdisk, tras "sincronizar ahora" las pistas listadas muestran su calificación importada (`docs/screenshots/R3-F5-rating-imported.png`); calificar una pista sin calificación previa desde Options cambia el valor en el lugar, sobrevive cambiar de pista y volver (`docs/screenshots/R3-F5-rating-set.png`), y **ambos casos** (importada y puesta a mano) persisten tras reiniciar el simulador de cero. Ver `DECISIONS.md` M-066 para el hallazgo real encontrado en el camino (la cola async de tagcache, la misma de R3-4, también aplica aquí). |
| C27 | Contrato v10 (tres familias): cada árbol hermano dormido (`/.firmware-aura/`, `/.firmware-moonlit/`) se detecta y Ajustes › General › "cambiar sistema" ofrece una fila por familia hermana; inerte ("no instalado") si no hay dormido — dos renombres (`/.rockbox`→`/.firmware-metro`, `/.firmware-<hermano>`→`/.rockbox`), respaldo `/rockbox.ipod` := el del entrante, marcador `/.aura/sync-pending.json` con `music: true` solo si la biblioteca cambió (v12), reinicio en seco | ✅ | R5 | Ítem nuevo de ronda 5 (M-090; generalizado a tres familias en M-093). La tabla de hermanos vive en `metro_firmware_families.c`, las rutas del cambio en `metro_settings.c` (regla de rutas de contrato); el marcador lo escribe `metro_sync_write_music_pending_marker()`. Antes de renombrar se vacía todo a disco (`metro_settings_save` + `settings_save` + `tagcache_shutdown` + `call_storage_idle_notifys(true)`) y el reinicio es `system_reboot()`, nunca el apagado normal. Verificado en simulador: árboles intercambiados, `aura.cfg` de Metro intacto en el dormido, raíz y marcador correctos; capturas `docs/screenshots/v0.6.0-cambiar-sistema-*.png`. Studio (ST-056) hace la misma secuencia desde el Mac y repara un cambio a medias al conectar |
| C28 | Contrato v11: `.rockbox/aura/install_manifest.cfg` (manifiesto de actualización selectiva) es de Studio — Metro lo ignora, no adopta ese nombre, y el build se mantiene reproducible para que el delta siga siendo ~5 archivos de 405 | ✅ | R5 | La actualización selectiva vive completa en Aura Studio (ST-058): compara los CRC32 del directorio central del `rockbox.zip` nuevo contra el manifiesto que él mismo dejó al instalar, extrae solo lo que cambió y borra lo que desapareció; sin manifiesto o ante cualquier duda cae a extracción completa. Del lado Metro las únicas reglas son (a) no usar ese nombre de archivo para otra cosa y (b) no introducir `__DATE__`/timestamps/aleatoriedad en nada que viaje dentro de `rockbox.zip` — la reproducibilidad del build es lo que mantiene chico el delta. Es por árbol (v10): viaja con su árbol al dormir/despertar y Studio nunca lo espeja |
| C29 | Contrato v15: base de datos tagcache **compartida** en `/.aura/tagcache/database_*.tcd` (fuera de todo árbol de firmware) con su sello `/.aura/tagcache/db_stamp.txt`; una base por dispositivo, no por familia | ✅ | R6 | M-095. `global_settings.tagcache_db_path` se fuerza a `AURA_SHARED_DB_DIR` en cada arranque desde `metro_apply_hygiene()` (antes de `tagcache_init()`); los `.tcd` de un árbol pre-v15 migran por `rename()` sin reconstrucción, o se borran si la compartida ya existía. `db_stamp.txt` deja de vivir en `.rockbox/aura/` (migra con la base o se borra). El sello se escribe también tras la reconstrucción de bootstrap (sin marcador de Studio) — antes solo lo escribía el ciclo de sync, y el primer cambio de familia reconstruía sin motivo. Studio **no** escribe ni borra nada bajo `/.aura/tagcache/`; el marcador `sync-pending.json` sigue siendo la única señal de "la biblioteca cambió". Verificado en simulador: migración, `stat` idéntico en el segundo arranque, `db_stamp.txt == library-stamp` |
| C30 | Contrato v15: miniaturas **compartidas** en `/.aura/thumbs/{albums,artists,photos}/<clave>.mth` (80×80 `fb_data` crudo, mismo formato que moonlit); clave de álbum estable `a-<crc32 ruta pista>-<mtime>` (no el seek de tagcache); `albums.dirty` como bandera interna de barrido | ✅ | R6 | M-096. Cualquier familia puede escribir y leer estos archivos: la clave se deriva del archivo de música (ruta + mtime que tagcache guarda por pista), así que una reconstrucción de la base no invalida nada y dos familias generan la misma clave para el mismo álbum. Huérfanos barridos (`metro_thumbs_gc`) en la siguiente entrada a Música tras un sync con música, un rebuild de bootstrap o la migración. Studio ignora este directorio (mismo principio que C23). Verificado en simulador: claves `a-<8hex>-<mtime>.mth`, `stat` idéntico tras borrar la base y reconstruir |
| C31 | Contrato v16: caché **maestra** compartida en `/.aura/art/{albums,artists,photos}/<clave>.art` (cabecera 16 B `'MAST'`+ancho+alto + RGB565 LE crudo, cuadrada — 130×130 álbumes/artistas, 80×80 fotos; `<clave>.none` marcador negativo de 0 bytes); un JPEG se decodifica UNA sola vez por dispositivo, por cualquier familia, nunca de nuevo; constructor en segundo plano (hilo de baja prioridad) la puebla completa desde el arranque, sin pantalla | ✅ | R6 | M-097. Mismas claves que C30 (álbum: idéntica a la de `.mth`; artista/foto: `r-`/`p-` + crc32 de la ruta de imagen + mtime). Todo consumidor (motor de tiles, Ahora Suena, el constructor) sigue la regla "maestra antes que JPEG": lee si existe, decodifica y escribe si no, honra `.none`. El `.mth` de 80px de Metro se deriva ahora de la maestra (filtro de caja si es de 130px, copia directa si ya es de 80px) — nunca de un segundo decode. Un mutex recursivo único (`metro_master_art_lock()`) serializa todo decode JPEG y todo recorrido de tagcache del proceso, porque ambos guardan su estado en estáticos únicos (`read_jpeg_file()`, `s_uniqbuf`) que dos hilos entrelazados corromperían. El constructor se pausa durante transiciones/animaciones, cede el disco a la reproducción cuando el buffer de audio está bajo, y no reintenta durante 2s tras cualquier entrada de usuario. Studio ignora este directorio (mismo principio que C23/C30); cualquier familia puede escribir y leer sus archivos indistintamente. Verificado en simulador: `/.aura/art/{albums,artists,photos}/` completo tras el arranque sin entrar a Música, cabeceras `MAST` válidas, `stat` idéntico en el segundo arranque, cuadrículas y Ahora Suena mostrando carátulas reales desde la maestra |
| C32 | Contrato v18: `/.aura/art/format.txt` — un entero decimal (v18: `2`) que versiona el FORMATO de todo lo derivado de una imagen fuente. Al arrancar, cada familia lo lee; si falta o es menor que el suyo, borra **todo** `/.aura/art/{albums,artists,photos}` y su caché L2 privada (en Metro, `/.aura/thumbs/`) y escribe su número. Studio nunca lo toca ni lo borra | ✅ | R7 | M-102. Ruta armada solo por `metro_settings_master_art_format_path()` (regla de rutas del CLAUDE.md); la lógica de versión vive en `metro_master_art_check_format_version()` y la purga en `metro_settings_purge_derived_caches()` (dueño de las dos raíces `/.aura/art` y `/.aura/thumbs`). Corre en `metro_main()` **entre** `metro_disk_handoff()` y `metro_master_art_builder_init()`: es el único punto del arranque donde la purga no compite con el constructor en segundo plano escribiendo maestras en el directorio que está por vaciarse. Un archivo ilegible (ausente, vacío, no numérico) cuenta como versión 1 — purgar cuesta una reconstrucción, conservar un tile mal derivado cuesta para siempre. **Medido en el simulador**: 2 048 archivos sembrados a mano en las seis subcarpetas, purgados en **10 ticks (~100 ms)**, muy por debajo del tope de ~2 s del plan maestro §A.3.2, con `format.txt` quedando en `2`; segundo arranque sin purga (idempotente) y caché repoblada por el constructor (31 archivos). El simulador escribe sobre el sistema de archivos del host (SSD), no sobre FAT en el disco del iPod: la cifra es una cota inferior y la lista de verificación en hardware la confirma |
| C33 | Contrato v18: el `<mtime>` de la clave de álbum `a-<crc32>.<mtime>` es `max(mtime de la pista representativa, mtime del `cover.jpg` hermano si existe)` | ✅ | R7 | M-102. Cierra la hipótesis (a) de D-338/M-096/D-055: hasta v16 una `cover.jpg` reescrita por Studio sin tocar ninguna pista no movía la clave, así que la maestra y el tile viejos sobrevivían para siempre. La regla es una función pura, `metro_master_art_album_mtime()` (`metro_master_art_format.c`, compila en host), cubierta por 7 comprobaciones nuevas en `test/test_master_art.c`; el mtime del hermano lo lee `metro_fsutil_file_mtime()` (Rockbox no tiene `stat()`: es un `opendir`+`dir_get_info` del directorio padre) y solo se consulta al resolver la clave, que ya está memoizada en un anillo de 64 entradas. El crc **no** cambia (lo que identifica al álbum sigue siendo la ruta de la pista); lo que cambia es cuándo deja de ser válido lo derivado. Sin `cover.jpg` el valor es 0 y la clave queda idéntica a la de v16 — una biblioteca con solo carátula embebida no revuelve su caché al actualizar. **Verificado en el simulador**: tras `touch` de `Music/Wheel & Click/Analog Dreams/cover.jpg` (sin tocar ninguna pista) la clave pasó de `a-031b464b.1787198376` a `a-031b464b.1788544800` y la maestra vieja quedó desalojada |
| C34 | Diferencia conocida (no un desvío del contrato): fotos en `/.aura/thumbs/photos/` — Metro sigue escribiendo y leyendo su `.mth` derivado de la maestra; moonlit.aura (D-072) dejó de escribirlo y lee la maestra de 80 px directo en el dibujo de la rejilla, con presupuesto de 4 lecturas por cuadro | 🟡 (evaluado, no adoptado esta ronda) | R7 | La maestra de una foto ya mide 80 px — igual que el tile — así que el `.mth` de fotos es, para Metro, una copia byte a byte de la maestra más una vuelta extra de E/S por foto (M-097 ya deriva `.mth` de la maestra, nunca de un segundo decode; para fotos esa derivación es una copia trivial). moonlit.aura eliminó ese paso y lee `/.aura/art/photos/<clave>.art` directo al dibujar la cuadrícula. **Compatible en las dos direcciones** — el formato y las claves de `/.aura/art` no cambian, y Metro puede seguir escribiendo `/.aura/thumbs/photos/` sin que eso rompa nada para moonlit ni para Studio (que ignora ambos directorios, C23/C30/C31). No se adoptó en esta ronda por precaución: moonlit encontró un bug real al implementarlo (encolar cuando se agota el presupuesto de lecturas del cuadro) y adoptarlo aquí sin la misma verificación de punta a punta que allá hubiera cargado ese mismo riesgo a esta ronda, que ya cerraba con las Fases 0-6 completas. Candidato para una ronda futura si el ahorro de E/S por foto llega a importar en hardware real |
| C35 | Contrato v19: `/.aura/settings.cfg` — ajustes compartidos entre Aura/Metro/moonlit (candado, brillo, retroiluminación, apagado automático, clicker, límite de volumen, ajuste de volumen/replaygain, idioma, apariencia oscuro/claro), texto plano `clave: valor` con cabecera `# aura-shared-settings v1` y `rev`/`updated_by` para desempatar quién escribió último. Studio nunca lo toca ni lo borra (mismo principio que `/.aura/art`) | ✅ | ronda "ajustes 2" | M-110 (plan maestro §A). Formato/parseo puros en `metro_shared_settings.c/.h` (host-testeados contra el vector A.3 literal: 13 claves conocidas + `clave_futura` preservada, archivo sin cabecera rechazado entero, `brightness: 999` ignorado sin abortar el resto — `test/test_shared_settings.c`, 43 comprobaciones); la ruta y toda la E/S viven en `metro_settings.c` (regla de rutas del CLAUDE.md), escritura atómica (`creat()`, regenera el archivo entero, nunca edita en sitio — mismo patrón que `aura.cfg`). Se aplica en el mismo punto que la hora (`metro_disk_handoff()`, boot y retorno de USB) si `rev` en disco es mayor que `shared_rev_applied` (nueva clave persistida en `aura.cfg`); se escribe de vuelta (`rev+1`, `updated_by: metro`) en cada punto de Ajustes que cambia una clave compartida y al restablecer. `brightness`/`backlight_timeout`/`idle_poweroff`/`volume_limit` ya viajan en la unidad nativa exacta de Rockbox (no hace falta traducir nada); `volume_limit` se redondea al nivel de Metro más cercano (00-15) para que el campo aplicado sea siempre uno que la fila de Ajustes pudiera haber producido. Como efecto colateral de mapear `replaygain` se encontró y corrigió un bug real, ya en producción desde M-103/v0.7.0: la fila "apagado" de Ajuste de volumen escribía `4` en vez de `REPLAYGAIN_OFF` (`3`), y `dsp_replaygain_update()` compara con `!=`, así que "apagado" nunca apagaba el procesamiento de verdad — ver `DECISIONS.md` M-110. **Verificado en el simulador**: arranque con el vector A.3 completo aplicó las 13 claves al estado vivo (capturas `docs/screenshots/ajustes-2/m110-a3-aplicado.png`, hub en inglés confirmando `language: en`, y `m110-settings-aplicado.png`, fila a fila con los valores del vector) y a `aura.cfg`/`config.cfg`; alternar el clicker desde Ajustes reescribió `settings.cfg` con `rev: 8`, `updated_by: metro` y `clave_futura: lo que sea` intacta (`m110-toggle-keyclick.png`). El candado (`screen_lock_enabled`) activado por el archivo compartido se refleja en el estado guardado de inmediato pero, igual que la hora, solo se hace cumplir a partir del **siguiente** arranque (`metro_screen_lock_run_if_active()` corre antes que `metro_disk_handoff()` dentro del mismo arranque, misma razón que M-068) — documentado, no un bug. Idiomas fr/de/ru/it: el código de dos letras viaja y se preserva en el archivo sin cambios, pero Metro aún no los aplica (`metro_lang_from_code()` los reconoce y devuelve `false` a propósito) hasta la Fase 3 de esta ronda |
